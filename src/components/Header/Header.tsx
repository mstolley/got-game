import styles from '../../styles/GotGame.module.css';

const Header = () => {
    return (
        <header className={styles.header}>
            <h1>GotGame</h1>
        </header>
    );
};

export default Header;